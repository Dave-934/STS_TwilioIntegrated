python-dotenv # --> for loading environment variables from .env file.
# vocode --> Not Required anymore, since Vocode isn't gving the desired results.
rasa # --> for NLU and conversational management.
deepgram-sdk # --> for speech-to-text (Deepgram ASR client).
requests # --> for making HTTP requests (HTTP client for Gemini & ElevenLabs API calls).
pydub # --> for audio manipulation (Audio processing library) (converting ElevenLabs output).
sounddevice # --> for recording audio from microphone (Audio recording library) (To play audio locally in Python).
numpy # --> For audio data manipulation (Numerical operations library).
grpcio # --> For audio streaming if needed by Deepgram (gRPC library).
